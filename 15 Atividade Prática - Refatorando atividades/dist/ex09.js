"use strict";
// Reescreva o exercício 3, utilizando apenas o SWITCH.
let tabela = Number(prompt('Digite o valor do produto:'));
let parcela = Number(prompt('Digite a quantidade de parcelas:'));
let txAvista = preco * 2.5 / 100;
let jrs6a10 = preco * 6 / 100;
let jrs11a15 = preco * 13 / 100;
let vrTotal = 0;
switch (parcelamento <= 0 || parcelamento >= 16) {
    case :
        document.write('Digitar um valor válido para calcular o parcelamento!');
        break;
    case parcelamento >= 16:
        document.write('Digitar um valor válido para calcular o parcelamento!');
        break;
}
